<h1> Listing users </h1>

<table>
    <tr>
        <th>User name</th>
    </tr>
    
    <% @users.each do |user| %>
        <tr>
            <td><%= link_to user.first_name + " " + user.last_name, user_path(user) %></td>
        </tr>
    <% end %>
</table>
