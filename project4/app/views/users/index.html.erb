<h1> Listing users </h1>

<table>
    <tr>
        <th>User name</th>
    </tr>
    
    <% @users.each do |user| %>
        <tr>
            <td><%= user.first_name + " " + user.last_name %></td>
        </tr>
    <% end %>
</table>
